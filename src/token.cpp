#include "token.hpp"

// Declaration of curentToken, tokenList
Token currentToken;
std::vector<Token> tokenSequence;
int tokenIndex = 0;
// Initialization of current position of token
int currentPos = 1;